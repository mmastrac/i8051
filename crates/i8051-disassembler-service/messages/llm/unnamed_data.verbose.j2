{% if inferred %}{{ at }}: {{ refs }} site{{ "s" if refs != 1 else "" }} load{{ "s" if refs == 1 else "" }} this address as a pointer (first {{ first }}), but it is unnamed. Read {{ first }} to decide.{% else %}{{ at }}: unnamed data. {{ refs }} site{{ "s" if refs != 1 else "" }} load{{ "s" if refs == 1 else "" }} it as a pointer (first {{ first }}). Read {{ first }} to help name it.{% endif %}