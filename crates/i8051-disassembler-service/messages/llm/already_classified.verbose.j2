{{ at }} is already {{ marked }}, covering {{ covering }}. Marking only applies to undefined bytes, so clear what is there first{% if cleared > asked %}. The clear takes all {{ cleared }} byte(s), past the {{ asked }} asked about, so restore the remainder straight after{% endif %}.