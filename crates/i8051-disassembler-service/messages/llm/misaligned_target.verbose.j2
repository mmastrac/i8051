{{ verb }} from {{ from }} to {{ to }} lands inside the instruction at {{ covering }}, not at its start. Either the bytes at {{ from }} are not code (e.g.: a branch decoded out of filler) or that instruction is decoded from the wrong offset. `peek` from both ends to decide