`{{ label }}` already labels {{ holder }}. Two addresses cannot share a symbol: the listing would be ambiguous and the assembly would not build. Pick a name that distinguishes this one from {{ holder }} (what it does differently, or its role relative to it), or move the name if this is its better home.