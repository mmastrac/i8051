{{ at }} is inside {{ barrier }}, which is marked {{ marked }}. A barrier stops auto-disassembly, so this would record a root, decode nothing, and report success while doing it. If these bytes are not code, the reference that made this look like an entry point is the thing to explain: the bytes that branch here may themselves be filler.