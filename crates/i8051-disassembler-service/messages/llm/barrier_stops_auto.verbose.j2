{{ at }} is inside {{ barrier }}, marked {{ marked }}. A barrier stops auto-disassembly, so this would record a root and report success without decoding anything. If these bytes are not code, explain the reference that made this look like an entry point. The bytes that branch here may be filler.