{{ at }} ({{ name }}) is named but has no note. Record in detail what it does and how it works with `set_note`. Later passes read the note{% if not has_comment %}. Add a one-line `set_comment` too if its purpose is clear{% endif %}