{{ at }} ({{ name }}) is named but has no note. Record what it does and how it works as a `set_note`, the detailed account future passes read{% if not has_comment %}. Add a one-line `set_comment` too if its purpose is clear{% endif %}