`{{ label }}` already marks {{ holder }} in this same routine. A local name is scoped to the routine that contains it, so it still has to be unique within it, but it is free to repeat in other routines.