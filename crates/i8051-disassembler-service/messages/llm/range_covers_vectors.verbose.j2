this range covers {% if vectors|length == 1 %}one{% else %}several{% endif %} of the CPU vectors: {{ vectors|join(", ") }}. Each is where the hardware starts executing, and each one's bytes are either a handler or filler. Marking a range would decide all of them at once. Decide them one at a time.