this range contains {% if targets|length == 1 %}an address{% else %}addresses{% endif %} that code branches to: {{ targets|join("; ") }}{% if omitted > 0 %}, and {{ omitted }} more{% endif %}. Marking it data would leave {% if sources == 1 %}an instruction{% else %}instructions{% endif %} branching into data, with the reference still in place. Settle the reference first. If the branch at {{ first_source }} is filler decoded as code, classify those bytes. That removes the reference, and this range can then be marked.