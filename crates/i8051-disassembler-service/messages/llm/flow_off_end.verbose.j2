code at {{ from }} runs past the end of mapped bytes, so the next instruction would come from nothing. Either {{ from }} is filler that decoded as code, or the image is incomplete and continues past what is loaded. `peek` it and decide