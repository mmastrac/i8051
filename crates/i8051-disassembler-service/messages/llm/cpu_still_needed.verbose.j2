the database is decoded against {{ cpu }}: {{ decoded }} bytes of code would stop being code, because nothing decodes without a CPU. Nothing in a disassembly session needs the CPU cleared: it is chosen once when the database is created.