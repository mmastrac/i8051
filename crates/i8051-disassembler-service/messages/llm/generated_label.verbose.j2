`{{ label }}` is the name this address already displays when it has none: storing it would retire the address from the naming worklist without settling it. A rough guess you refine later beats the generated form.